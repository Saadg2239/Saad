package Elements;

import Pages.PageBase;
import org.openqa.selenium.WebDriver;

public class ResponsesElements extends PageBase {
    // Constructor
    public ResponsesElements(WebDriver driver) {
        super(driver);
    }
}
